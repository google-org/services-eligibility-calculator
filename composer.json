{
  "name": "google.org/service-management-portal",
  "description": "Google.org Service Management Portal",
  "type": "project",
  "license": "Apache-2.0",
  "require": {
    "php": "^7.1.3",
    "ext-ctype": "*",
    "ext-iconv": "*",
    "ext-intl": "*",
    "ext-ldap": "*",
    "ext-mbstring": "*",
    "ext-gd": "*",
    "ext-pdo_sqlite": "*"
  },
  "scripts": {
    "post-install-cmd": [
      "/bin/bash -c '[[ ! -z \"$COMPOSER_PATH\" ]] || exit 0 && composer install -d $COMPOSER_PATH'"
    ]
  }
}
